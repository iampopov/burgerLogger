<h1>Burgers!</h1>

<h2>Burgers that are not eaty!</h2>

<ul>
  {{#each burgers}}
  {{#unless eaty}}
  {{> burgers/burger-block eat=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are eaty!</h2>

<ul>
  {{#each burgers}}
  {{#if eaty}}
  {{> burgers/burger-block eat=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaty">Eaty?</label><br>
    <input type="radio" name="eaty" value="1" checked> Eaty!<br>
    <input type="radio" name="eaty" value="0"> Awake
  </div>

  <button type="submit">Add burger</button>
</form>